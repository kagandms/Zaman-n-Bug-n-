# Archive

This directory contains deprecated and unsupported reference scripts kept only for historical comparison during the v4 rewrite.

- `legacy_main.py`: pre-refactor synchronous bot entrypoint
- `debug.py`: old local debugging helper tied to the legacy flow

These files are not part of the supported runtime path. Production entrypoint is `python -m src.main`.
